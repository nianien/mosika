# 仓库协作指南

## 项目结构

本项目是基于 JDK 21 的 Maven 多模块规则引擎。

- `mousika-core/` 是纯内核，负责 DSL 解析、规则语法树、执行上下文、规则求值和 UDF；ANTLR 语法位于 `src/main/antlr4/Rule.g4`。
- `mousika-ui/` 不包含真正的页面或布局，只把内核语法树转换为前端容易序列化、递归遍历和渲染的数据结构。
- 生产代码和测试分别位于各模块的 `src/main/java`、`src/test/java`；不要修改 `target/generated-sources/` 中的生成代码。

## 核心设计理念

`RuleNode` 及其组合节点是规则语义的唯一来源。`->` 表示串行，`=>` 表示并行，条件、逻辑组合和 `hits(min,max,...)` 均可递归嵌套；`_` 表示命中区间的一侧不设边界，例如 `hits(2,_,a,b,c)`。`∅` 只用于稳定表达 `SNode`、`PNode` 等结构，不参与业务计算。

修改前必须沿实际链路核对 `Rule.g4`、`DefaultRuleVisitor`、具体 `RuleNode.eval()`、`UINodeAdapter` 和回归测试。不要仅凭方法名推断语义：例如 `ParNode.next()` 是协变重写，含义是追加并行节点，并非创建串行节点。保持现有组合模型，避免为兼容旧实现增加重复语法树或适配层。

## 执行结果契约与变更边界

必须保持现有结果模型的职责分离：`EvalResult.matched` 表示节点的匹配或控制状态，`EvalResult.result` 只传递当前节点具有明确语义的业务返回值。不要把诊断信息、状态对象等控制信息塞进 `result`，也不要在未对齐前为 `EvalResult`、`RuleResult`、`NodeResult` 增加字段、包装类型或改变序列化结构。

`AndNode`、`OrNode`、`HitsNode` 是逻辑节点，负责解释子节点的匹配结果并按各自规则短路。`SerNode`、`ParNode` 是执行结构：前者依次执行所有子节点，后者并发执行所有子节点并等待完成；二者不根据子节点的 `matched` 中止或聚合真假，正常完成统一返回 `result=null、matched=true`。规则未命中不等于执行失败，异常继续通过现有机制传播。

评审结果相关问题时，先核对现有字段职责、节点语义和调用方，再判断最小局部修复是否足够。诊断、追踪属于独立能力；如确有新增需求，应先与维护者确认，不得直接改变核心执行结果的语义或全局模型。

## UI 树与递归约束

UI 节点表达结构而非业务语义或页面布局：`ANode.next` 表示后继，`SNode/PNode.branches` 表示串行/并行子结构，`CNode.action` 表示命中分支，`DNode` 保存有序条件分支及可选默认分支，`JNode.rule` 可包含由 `RNode`、`LNode`、`HNode` 组成的递归规则树。这些节点可以相互嵌套，前端自行决定横排、竖排、折叠或展开；不得因画布空间改变树的层级和 DSL 语义。

这套模型的目标不是还原传统流程图，而是把控制语义显式表达为可递归组合的树。`SNode`、`PNode`、`DNode`、`JNode` 都是一等结构节点，也是各自子树的虚拟根：串行、并行、决策和判断不能通过节点的横纵位置、连线方向或隐含汇合关系来推断。显式结构虽然会增加少量视觉节点，却能保证任意组合仍然是一棵树，例如 `S(P(a,b),P(c,d))` 可以直接表示“第一组并行全部完成后，再执行第二组并行”，不需要分叉、汇合、多入边或其他 DAG 结构。

所有同类子树都必须满足可替换性：任意 `FlowNode` 子树可以作为另一个流程组合的分支，任意 `RNode` 子树可以作为另一个规则组合的子规则。`JNode` 是规则递归与流程递归之间的固定连接点，左侧始终是 `Rule`，右侧始终是 `Flow`；单规则和复合规则都可以使用同一判断形态，区别只在左侧规则树的复杂程度。`∅` 用于稳定表示单分支结构，使单分支和多分支沿用同一套组合、遍历和序列化逻辑。

从语言工程角度，这是一种以 `Flow` 为宿主、以 `Rule` 为嵌入式子语言的多分类 AST（multi-sorted AST）。两个递归域是单向嵌入而非相互递归：`Flow` 可以通过 `JNode` 引用 `Rule`，`Rule` 只能继续包含纯规则节点，不能反向引用 `Flow`。`JNode` 是两个语法域之间的嵌入点和边界节点。

前端可以对两个语法域采用异构投影：`JNode` 与 `JNode.rule` 可以在主画布统一投影为一个规则节点，主画布不展示可能很长的 DSL 或规则摘要，完整规则树和名称在单一弹窗或独立子编辑器中展示。规则名称属于 `JNode.rule` 指向的实际规则根节点，不属于外层 `JNode`：简单规则表示为 `JNode(RNode, Flow?)`，主画布显示 `RNode` 的名称；复合规则表示为 `JNode(LNode/HNode, Flow?)`，根节点有名称时显示名称，未命名时显示“复合规则”。两者区别只在规则子树复杂度，使简单规则能在同一个规则递归域内继续扩展；不得回退展示表达式。这种视觉合并不得删除底层 `JNode`、改变 `rule/action` 边界或破坏底层树结构及 JSON 往返。`JNode.action` 属于主流程递归，后续流程还可能再次包含 `JNode`，必须在主画布中原位展开。不得用弹窗承载流程递归，否则交互层级会随树深度退化为弹窗嵌套。弹窗只负责规则子树或原子属性等有明确边界的局部编辑，不承担流程导航。

当前编辑器对两种投影采用不同阅读方向：主画布中的 `Flow` 从上到下展开，规则弹窗中的 `Rule` 从左到右展开、同级规则纵向排列。该方向差异仅用于匹配流程顺序和逻辑表达式的阅读习惯，不得写入节点模型或参与执行语义。

主画布把 `SNode`、`PNode`、`DNode` 三种流程组合统一投影为圆形的“串、并、分”。“分”表示 `DNode` 的有序互斥选择：按顺序检查条件分支，命中首个后停止，并可带默认分支；它不是普通的无条件分叉。该简称只属于前端视觉语言，不得替代节点类型或改变执行契约。

演示编辑器的新建入口应统一用 `JNode` 表达业务规则，不再把简单规则作为独立的 `CNode` 创作模式；已有 `CNode` 仍是合法模型并应保持兼容。该约束属于编辑策略，不得据此删除 `CNode`、改变其 `action` 语义或破坏既有 JSON 往返。

规则名称是规则节点自身的 UI 元数据：定义在 `RNode.name`，`LNode/HNode` 通过继承获得，不能放在外层 `JNode`。名称不参与 `ruleExpr()` 和求值；未设置时不写入 JSON。UI 树及其 JSON 是编辑和持久化的唯一事实来源；`UINodeAdapter` 只保留 `toRule()`，把 UI AST 单向编译为执行用的 `RuleNode`。转换不保留 `label`、`name` 等 UI 元数据，也不提供从 `RuleNode` 反向恢复 UI 树的能力。旧 `CNode` 仍需支持 JSON 反序列化和 `toRule()`。

新增 `JNode` 后应直接进入统一规则编辑器；通用流程节点编辑器不得另设 `JNode` 的 DSL 输入。原子规则不能在该编辑器内临时创建或手填表达式，而要从后端已有 `RuleDefinition` 中选择：`RNode.expr` 保存稳定的 `RuleDefinition.ruleId` 引用，`RNode.name` 是节点自身可编辑的 UI 名称，初始值可取 `RuleDefinition.desc`，后端定义的 `expression` 不复制进 UI 树。简单规则和复合规则必须共用创建、展示和后续编辑路径。

规则编辑器添加内容时应区分“引用一条已有规则”和“创建与/或组合节点”。前者从规则定义下拉框选择；后者一次选择两条已有规则，形成满足最小元数约束的 `LNode`。向已有组合添加内容时追加到子规则末尾，位置再通过拖拽或行内上下移调整；顺序属于有序子节点列表，不通过画布坐标推断。

规则弹窗的右侧编辑器是常驻的直接编辑面板：选中规则节点后即可在此就地查看并修改该节点，没有独立的“查看/编辑”切换，也没有规则节点右键菜单。逻辑节点在此切换“与/或”并对直接子规则排序，组合节点通过“添加规则”增补子规则，任意规则可在此删除。面板内的改动即时写入规则树做实时预览，最终由弹窗顶部“保存”事务化提交、“取消”或 Esc 回滚到打开时的规则子树快照。

配置根规则在右侧编辑器内联完成；向已有组合追加子规则时打开一个规则列表面板（当前实现为小弹窗）。面板默认包含一条已有规则，通过“添加规则”继续增加：只有一条时直接创建 `RNode`；两条及以上时才显示统一的与/或选择，并创建包含全部所选规则的 `LNode`。删除回一条时应自动隐藏与/或并退化为单规则。新增子规则追加到组合末尾，位置再通过拖拽或行内上下移调整；组合名称创建后按需编辑，不与结构创建同时填写。

当前直接编辑面板尚未提供把单个已存在原子规则就地“转复合”的入口；以下为该能力落地时须遵循的语义约束。单一规则转复合规则时必须在原 `JNode` 内替换规则根：创建未命名的 `LNode`，把原 `RNode` 连同名称、规则 ID 引用和取反状态完整保留为第一个子规则，再由用户从已有 `RuleDefinition` 中选择第二个原子规则。不得生成虚假的占位规则，不得重建或替换外层 `JNode`，也不得丢失其 `action` 和流程位置。`HNode` 仍是合法规则结构并保持兼容，但当前与/或转换入口不负责创建 `hits`。

不要为了减少画布节点而删除显式串行节点，或改用“纵向表示串行、横向表示并行”等布局约定承载语义。否则并行后的公共后续流程必然需要显式或隐式汇合点，树会退化成需要推断作用域的图。视觉优化应限定在节点尺寸、间距、折叠和递归布局上，不得牺牲结构节点、子树边界和 JSON 往返一致性。

节点模型必须保持“规则求值”和“流程执行”的边界：规则子树由原子条件、与、或和 `hits` 等纯规则节点递归组成，只负责产生匹配结果；串行、并行、决策、判断、条件和动作属于流程节点。判断节点的规则侧只能接收纯规则子树，命中侧才能接收任意流程节点；不得把带执行分支的判断节点再次当作外层规则使用，避免在求值过程中意外执行其命中分支。

各节点的递归闭包和分支约束如下：

- `SNode/PNode` 包含一个或多个 `FlowNode` 分支；串行节点按顺序执行，并行节点并发执行并等待全部分支完成。横向扩展只是一种前端布局方式，不属于执行语义。
- `ANode.next` 为零个或一个任意 `FlowNode` 后继。
- `CNode.action` 为零个或一个任意 `FlowNode`。`action=null` 时是纯条件，非空时表示条件为真后执行该分支。
- `JNode.rule` 是纯规则树，`action` 为零个或一个任意 `FlowNode`；规则为真时才执行 `action`。
- `LNode` 的与、或组合至少包含两个纯规则子节点，并保持各自的短路语义；不得直接包含动作、串行、并行等流程节点。
- `DNode.branches` 是有序的 `CNode/JNode` 条件分支，按顺序命中第一个后停止；`DNode.action` 是所有条件均未命中时执行的可选默认分支，可以是任意 `FlowNode`，不能限制为 `ANode`。无默认分支且全部条件未命中时不执行后续分支。

从类型上可将递归关系概括为：

```text
Flow = A(next?) | S(Flow+) | P(Flow+) | C(rule, Flow?) | J(Rule, Flow?) | D((C|J)+, Flow?)
Rule = R | And(Rule, Rule+) | Or(Rule, Rule+) | Hits(bounds, Rule+)
```

修改节点时必须同步检查：`toRule()` 的单向编译、`TreeNode.visit()` 的全部递归边、`NodeTypeResolver` 的 JSON 类型映射，以及“UI → JSON → UI → Rule”的结构与执行语义一致性。不得根据 `RuleNode` 或 DSL 猜测恢复 UI 结构；不要重新引入已合并删除的 `tree2`。

## 构建与测试

- `mvn clean test`：重新生成 ANTLR 代码并运行全部测试。
- `mvn -pl mousika-core test`：只验证内核。
- `mvn -pl mousika-ui -am test`：验证 UI 树及其内核依赖。
- `mvn clean package`：测试并生成各模块 JAR。

解析、执行、并发上下文或树转换的修改必须增加针对性回归测试。测试类使用 JUnit 5，命名为 `*Test`。

## 编码与提交

Java 使用四空格缩进，包名保持在 `com.skyfalling.mousika` 下；类、方法、常量分别使用大驼峰、小驼峰和全大写下划线命名。保持相邻代码的导入和注释风格，不引入无必要的抽象或依赖。

提交信息使用简短中文主题，一个提交聚焦一个问题。合并请求需说明影响模块、语义变化、验证命令；涉及 UI 树时附代表性 DSL、JSON 或结构示例。
