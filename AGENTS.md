# AGENTS.md

## 项目性质

Mosika 是规则引擎内核，`mosika-core` 全部是**进程内代码**，
不是对外 SDK，没有跨进程/跨系统调用。
所有调用方都在同一个仓库里，参数来源可追溯。

---

## 一、修改范围

- 只改任务明确要求的方法。其他方法、其他文件，**一行都不许动**。
- 认为有改进空间的地方，在回复里说明，**不要直接改**。
- 动手前先列出打算改哪些文件的哪些方法，等确认后再写。
- 不要顺手做重构、重命名、抽接口、提公共方法。

---

## 二、参数校验：先判断边界

加校验前，先回答一个问题：**这个参数从哪来的？**

**信任边界内 —— 不加任何校验：**
- private / package-private 方法
- 同模块内部调用的 public 方法
- 构造器接收 varargs（JVM 保证非 null）
- 参数由本仓库其他代码构造并传入

理由：调用方在同一个仓库里，传错就是 bug，应该在测试里暴露，
不是靠运行时判断兜住。下一行解引用照样 NPE，栈顶就在出错的位置。

**信任边界上 —— 才加校验：**
- HTTP / RPC 入口（mosika-web 的 Controller 层）
- 解析外部输入：DSL 文本、JSON 参数、数据库读出的规则定义
- 值被存进字段、隔很久才解引用（此时 NPE 栈里已无源头信息）

**具体禁止：**
- 禁止 `Objects.requireNonNull` 出现在 core 的构造器和内部方法里
- 禁止对 varargs 参数做 null 检查
- 禁止同一个值在调用链上被反复检查
- 禁止为"防御"而加 try-catch 吞异常

---

## 三、集合与不可变

- **禁止 `List.copyOf` / `Set.copyOf` / `Map.copyOf`**，除非明确说明这是对外
  API 边界且能说清被篡改的具体场景。
- 禁止把可变类改成不可变。
- 禁止加防御性拷贝。
- 禁止给已有的可变字段套 `unmodifiableList` 之类的包装。

`RuleNode` 的实现类（AndNode / OrNode / NotNode / CompositeNode 等）
是**构建器语义**，节点内部集合就是要可变的。

---

## 四、方法契约：绝对不许改

以下任何一条都属于破坏性变更，禁止在未明确要求时改动：

- 改返回值语义。特别是：**返回 `this` 的方法必须继续返回 `this`**，
  不许改成返回新对象。`RuleNode.and()` / `or()` 是原地追加 + 返回自身，
  这是接口契约，所有实现类必须一致。
- 改方法签名、参数顺序、返回类型
- 把原地修改改成拷贝-新建
- 改字段可见性

`final` 只用于"防止字段引用被重新赋值"，
不要连带把集合内容也改成不可变——这是两件事。

---

## 五、性能约束

`eval()` 及其调用链是求值热路径，规则解析期的节点组装也是高频路径。

- 链式调用的方法里禁止 O(n) 拷贝——`and()` 被调 n 次就是 O(n²)
- 热路径上新增任何堆分配，必须在回复里说明理由
- 不要为了"看起来干净"引入 Stream / Optional 替换现有循环

---

## 六、代码风格

- 不要加 Javadoc，除非要求
- 不要加 `// 遍历所有节点` 这种复述代码的注释
- 不要为单一实现抽接口
- 不要加工厂、建造者、策略类等未被要求的抽象层
- 不要动 lombok 注解
